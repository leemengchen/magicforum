$('#flash-messages-container').html("<%=j render partial: 'partials/flash_messages' %>")

<% if @comment.destroyed? %>
  $('#comments').html("<%=j render partial: 'comment', locals: { comment: @comment, post: @comment.post, topic: @topic } %>")
<% end %>
